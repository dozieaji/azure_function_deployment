name: "Terraform-Plan-Infra-Plan"

on:
  push:
    branches:
      - feature

jobs:
  terraform:
    name: "Terraform"
    runs-on: ubuntu-latest
    env:	
      AWS_ACCESS_KEY_ID: 'Q/hKL+nQ7PmBU6EeKZyz1QxNlaNryC0YD4gMSsgXImIbRRc7++tKa15oXA+pf+tDtU5z5I50e8Re8odhIZQRWg=='
      AWS_SECRET_ACCESS_KEY: 'a31ceb26edcd449eb0bafc4da232c132'
      AWS_REGION: 'us-east-1'

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v1

      - name: Terraform Init
        id: init
        run: terraform init

      - name: Terraform Plan
        id: plan
        if: github.event_name == 'push'
        run: terraform plan -no-color